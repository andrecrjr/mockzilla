---
title: Stateful Workflows
description: Build intelligent API simulations with persistent state and business logic.
---

# Stateful Workflows

Go beyond static mocks. Workflow Mode lets you build smart API simulations that remember previous requests. This is perfect for testing complex user journeys like a shopping cart, a multi-step checkout, or a full authentication flow.

<Alert>
  <AlertTitle>Static Mocks vs. Workflows</AlertTitle>
  <AlertDescription>
    Static mocks always return the same data. Workflows use a **mini-database** and **state variables** to return different responses based on what happened in previous steps.
  </AlertDescription>
</Alert>

---

## Why Workflow Mode?

Go beyond static mocks. Build stateful, intelligent API simulations.

<div className="bg-primary/5 border border-primary/20 rounded-lg p-4 mb-6">
  <div className="font-bold text-sm text-primary mb-2 flex items-center gap-2">
    Philosophy: Action-Driven State
  </div>
  <div className="grid md:grid-cols-2 gap-4 text-xs">
    <div className="space-y-1">
      <span className="font-semibold text-red-500">❌ Don't: CRUD State</span>
      <p className="text-muted-foreground">
        Creating endpoints just to set data.<br/>
        Ex: <code>POST /set-data</code>
      </p>
    </div>
    <div className="space-y-1">
      <span className="font-semibold text-green-500">✅ Do: Business Logic</span>
      <p className="text-muted-foreground">
        Transitions trigger state changes.<br/>
        Ex: <code>POST /checkout</code> updates DB.
      </p>
    </div>
  </div>
</div>

<div className="grid md:grid-cols-2 gap-6 mb-6">
  <div className="space-y-3">
    <h3 className="font-semibold text-lg flex items-center gap-2">
      <span className="text-muted-foreground line-through">Static Mocks</span>
      <Badge variant="secondary">The Old Way</Badge>
    </h3>
    <ul className="space-y-2 text-sm text-muted-foreground">
      <li className="flex gap-2">❌ Always returns the same JSON.</li>
      <li className="flex gap-2">❌ No memory of previous requests.</li>
      <li className="flex gap-2">❌ Impossible to test logical flows.</li>
    </ul>
  </div>
  <div className="space-y-3">
    <h3 className="font-semibold text-lg flex items-center gap-2">
      <span className="text-primary">Workflow Mode</span>
      <Badge>The New Way</Badge>
    </h3>
    <ul className="space-y-2 text-sm">
      <li className="flex gap-2">✅ **Stateful:** Remembers variables.</li>
      <li className="flex gap-2">✅ **Dynamic:** CRUD on in-memory DB.</li>
      <li className="flex gap-2">✅ **Smart:** Context-aware responses.</li>
    </ul>
  </div>
</div>

---

## The "Action-Driven" Philosophy

In Mockzilla, we follow a simple rule: **State variables must not be endpoints.**

In a real app, you rarely "set the database status to 'paid'" via a direct API call. Instead, you "process a payment," and the *result* is that the status becomes 'paid'. Mockzilla workflows mirror this:

- ❌ **Bad:** Creating a `POST /set-paid` endpoint to manually update a variable.
- ✅ **Good:** Creating a `POST /checkout` transition that internally updates multiple state variables and DB tables as a side effect of the action.

---

## Core Concepts

<div className="grid md:grid-cols-3 gap-4 my-8">
  <Card className="p-4 border-l-4 border-l-blue-500">
    <CardTitle className="text-sm flex items-center gap-2">Scenario</CardTitle>
    <p className="text-xs text-muted-foreground">
      An isolated sandbox for state and variables.
    </p>
  </Card>
  <Card className="p-4 border-l-4 border-l-green-500">
    <CardTitle className="text-sm flex items-center gap-2">Transition</CardTitle>
    <p className="text-xs text-muted-foreground">
      Rules defining "WHEN request X, THEN do Y".
    </p>
  </Card>
  <Card className="p-4 border-l-4 border-l-purple-500">
    <CardTitle className="text-sm flex items-center gap-2">Mini-DB</CardTitle>
    <p className="text-xs text-muted-foreground">
      Temporary JSON storage for each scenario.
    </p>
  </Card>
</div>

### 1. Scenarios
A **Scenario** is an isolated sandbox. Each scenario has its own independent state and mini-DB. You can have a "Happy Path" scenario and a "Payment Failed" scenario running side-by-side.

### 2. Transitions
A **Transition** is a rule that defines how Mockzilla should react to a request. It consists of:
- **Path & Method:** e.g., `POST /orders/:id`.
- **Conditions:** Does this rule apply to the current request?
- **Effects:** What changes should happen to the state or DB?
- **Response:** What should be returned to the client?

### 3. State & Mini-DB
- **State (`state`)**: A simple key-value store for variables like `isLoggedIn` or `currentUser`.
- **Mini-DB (`db`)**: A collection of JSON tables (arrays) for storing lists of items like `orders`, `products`, or `cart_items`.

---

## Deep Dive: Core Concepts

<Accordion type="single" collapsible className="space-y-4">
  <AccordionItem value="wf-transitions">
    <AccordionTrigger>Transitions & Conditions</AccordionTrigger>
    <AccordionContent>
      <div className="space-y-4 pt-2">
        <p className="text-sm text-muted-foreground">
          A transition links an incoming request to effects and a response.
        </p>
        
        <div className="border rounded-lg p-4 bg-muted/20">
          <h4 className="font-semibold text-sm mb-3">Condition Builder</h4>
          <div className="grid grid-cols-2 gap-4 text-xs">
            <div>
              <span className="block font-medium mb-1">Selectors</span>
              <ul className="space-y-1 font-mono text-muted-foreground">
                <li>input.body.field</li>
                <li>input.query.param</li>
                <li>input.headers.auth</li>
                <li>input.params.id</li>
              </ul>
            </div>
            <div>
              <span className="block font-medium mb-1">Operators</span>
              <ul className="space-y-1 font-mono text-muted-foreground">
                <li>==, !=</li>
                <li>&gt;, &lt;, &gt;=, &lt;=</li>
                <li>exists, contains</li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </AccordionContent>
  </AccordionItem>

  <AccordionItem value="wf-interpolation">
    <AccordionTrigger>Dynamic Responses & Interpolation</AccordionTrigger>
    <AccordionContent>
      <div className="space-y-4 pt-2">
        <p className="text-sm text-muted-foreground">
          Inject data using <code>{`{{ var }}`}</code> syntax.
        </p>
        <div className="grid gap-2 text-sm">
          <div className="border p-2 rounded flex justify-between items-center">
            <span className="font-mono text-xs text-primary">{`{{ input.body.id }}`}</span>
            <span className="text-xs text-muted-foreground">Echo request data</span>
          </div>
          <div className="border p-2 rounded flex justify-between items-center">
            <span className="font-mono text-xs text-primary">{`{{ state.token }}`}</span>
            <span className="text-xs text-muted-foreground">Stored variable</span>
          </div>
        </div>
      </div>
    </AccordionContent>
  </AccordionItem>
</Accordion>

---

## 🔗 Path Matching

Transitions support both exact and parameterized paths:

| Path | Example Match | Accessing Parameters |
|------|---------------|----------------------|
| `/users` | `/users` | (None) |
| `/users/:id` | `/users/42` | `{{input.params.id}}` |
| `/cart/:sku/add` | `/cart/SKU-123/add` | `{{input.params.sku}}` |

---

## 📝 Interpolation Reference

Inject dynamic values into your **Effects** or **Responses** using the `{{ path.to.value }}` syntax.

<Table>
  <TableHeader>
    <TableRow>
      <TableHead>Source</TableHead>
      <TableHead>Syntax</TableHead>
      <TableHead>Description</TableHead>
    </TableRow>  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell><Badge variant="outline">Input</Badge></TableCell>
      <TableCell>`{{input.body.id}}`</TableCell>
      <TableCell>Value from the request body.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><Badge variant="outline">Query</Badge></TableCell>
      <TableCell>`{{input.query.ref}}`</TableCell>
      <TableCell>Value from URL query parameters.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><Badge variant="outline">Params</Badge></TableCell>
      <TableCell>`{{input.params.id}}`</TableCell>
      <TableCell>Value from a `:id` path segment.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><Badge variant="outline">Headers</Badge></TableCell>
      <TableCell>`{{input.headers.authorization}}`</TableCell>
      <TableCell>Value from HTTP headers.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><Badge variant="outline">State</Badge></TableCell>
      <TableCell>`{{state.token}}`</TableCell>
      <TableCell>A variable stored in the scenario state.</TableCell>
    </TableRow>
    <TableRow>
      <TableCell><Badge variant="outline">Database</Badge></TableCell>
      <TableCell>`{{db.items}}`</TableCell>
      <TableCell>An entire table from the mini-DB.</TableCell>
    </TableRow>
  </TableBody>
</Table>

---

## 🛠️ Logic & Control Flow

### Conditions
Conditions determine if a transition fires.

| Type | Description | Example |
|------|-------------|---------|
| `eq` | Equals | `input.body.role` == `"admin"` |
| `exists` | Field exists | `input.headers.auth` exists |
| `contains` | Array contains | `db.roles` contains `"editor"` |

### Effects
Effects modify your data when a transition is triggered.

| Type | Description |
|------|-------------|
| `state.set` | Update state variables. |
| `db.push` | Add a new item to a table. |
| `db.update` | Find rows and update them. |
| `db.remove` | Delete matching rows. |

---

## 🚀 Example: Shopping Cart Flow

### Step 1: Add to Cart
- **Path:** `POST /cart`
- **Effect:** `db.push` to table `cart` with value `{{input.body}}`.
- **Response:** `{"success": true}`

```bash
# Add item
curl -X POST http://localhost:3000/api/workflow/exec/my-store/cart \
  -H "Content-Type: application/json" \
  -d '{"id": 1, "name": "Coffee Beans"}'
```

### Step 2: List Cart
- **Path:** `GET /cart`
- **Response:** `{{db.cart}}`

```bash
# Get cart items
curl http://localhost:3000/api/workflow/exec/my-store/cart
```

---

## 🔌 Using your Workflow

To trigger your workflow, use the execution endpoint:

```bash
# /api/workflow/exec/{scenario-slug}/{path}
```

<Alert variant="info" className="mt-8">
  <AlertTitle>Debugging Tip</AlertTitle>
  <AlertDescription>
    Use the **State Inspector** in the dashboard to see real-time state and DB tables.
  </AlertDescription>
</Alert>
